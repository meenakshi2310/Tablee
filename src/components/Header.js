import React, { useState } from 'react'

const Header = () => {
    // const [header, setHeader]=useState([
    //     {
    //         key:id,
    //         value:age
    //     }
    // ]);

  return (
    <div>

    </div>
  )
}

export default Header